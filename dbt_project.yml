name: imdb_analytics
version: '1.0.0'

profile: imdb_duckdb

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
docs-paths: ["docs"]

clean-targets:
  - target
  - dbt_packages

models:
  imdb_analytics:
    # Push model/column descriptions from schema.yml to DB as COMMENT ON TABLE/COLUMN (DuckDB/MotherDuck).
    +persist_docs:
      relation: true
      columns: true
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: view
      +schema: marts
